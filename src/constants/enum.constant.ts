export enum ROLE {
  ADMIN = "admin",
  STUDENT = "student",
  GUEST = "guest",
}

export enum STATUS {
  ACTIVE = "active",
  ENABLE = "enable",
  INACTIVE = "inactive",
  DELETED = "deleted",
}

export enum NOTIFI_TYPE {
  DEFAULT = "default",
  ATTENDANCE = "attendance",
  RESULT = "result",
}

export enum TERM_TYPE {
  MAIN = "main",
  INTERVIEW = "interview",
  BOOK = "book",
  RESULT = "result",
  PAY = "pay",
}
